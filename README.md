# ScamGuard AI

## Streamlit UI

Run the navigable prototype from this repository with:

```bash
python -m pip install -r requirements.txt
streamlit run streamlit_app.py
```

The UI includes Shield, Scan, History, and Pro views. The scan view has a local demo detector so it remains usable before the FastAPI service is connected.